apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: servicemonitor-stock-service
  namespace: monitoring
  labels:
    serviceapp: stock-service
    release: prometheus-operator
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: nautible-app-ms-stock
  endpoints:
  - port: http-nautible-app-ms-stock-port
    interval: 30s
    path: /q/health
  namespaceSelector:
    matchNames:
    - nautible-app-ms