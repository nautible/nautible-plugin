apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: delivery-alert.rules
  namespace: monitoring
  labels:
    app: kube-prometheus-stack
    release: prometheus-operator
spec:
  groups:
  - name: rules-delivery-alert1
    rules:
    - alert: ApplicationDown
      expr: >-
          count(kube_pod_status_phase{phase="Running", namespace="nautible-app-ms", pod=~"nautible-app-ms-delivery.*"}) < 1
      for: 5m
      labels:
        severity: critical
      annotations:
        message: >-
          {{ $labels.job }}/{{ $labels.service }} targets in {{ $labels.namespace }} namespace are down.
  - name: rules-delivery-alert2
    rules:
    - alert: ApplicationError
      expr: >-
          ( count (delivery_log_error_total) > 0 ) or ( count (delivery_log_severe_total) > 0 )
      for: 1m
      labels:
        severity: critical
      annotations:
        message: >-
          {{ $labels.job }}/{{ $labels.service }} targets in {{ $labels.namespace }} namespace are application error.