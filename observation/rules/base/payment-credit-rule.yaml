apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: payment-credit-alert.rules
  namespace: monitoring
  labels:
    app: kube-prometheus-stack
    release: prometheus-operator
spec:
  groups:
  - name: rules-payment-credit-alert1
    rules:
    - alert: PaymentCreditApplicationDown
      expr: >-
          count(kube_pod_status_phase{phase="Running", namespace="nautible-app-ms", pod=~"nautible-app-ms-payment-credit.*"}) < 1
      for: 5m
      labels:
        severity: critical
      annotations:
        message: >-
          {{ $labels.job }}/{{ $labels.service }} targets in {{ $labels.namespace }} namespace are down.
  - name: rules-payment-credit-alert2
    rules:
    - alert: PaymentCreditApplicationError
      expr: >-
          ( count (payment_credit_log_error_total) > 0 ) or ( count (payment_credit_log_severe_total) > 0 )
      for: 1m
      labels:
        severity: critical
      annotations:
        message: >-
          {{ $labels.job }}/{{ $labels.service }} targets in {{ $labels.namespace }} namespace are application error.